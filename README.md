# 32bit python env (using anaconda)

```bash
conda create -n py_32
conda activate py_32
conda config --env --set subdir win-32
conda install python=3.10
pip install numpy~=1.0
pip install pandas==2.0.3
pip install pykiwoom
```
